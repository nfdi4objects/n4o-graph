# Shows the english and german name of all objects and how many of its kind exist in the data
PREFIX crm: <http://www.cidoc-crm.org/cidoc-crm/>

SELECT ?name (COUNT(*) as ?count){
#  GRAPH <https://graph.nfdi4objects.net/collection/1> {
    ?object crm:P1_is_identified_by [ crm:P90_has_value ?name ] .
#  }
} 
GROUP BY ?name
ORDER BY desc(?count)
